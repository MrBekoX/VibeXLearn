# Temel güvenlik
requirepass ${REDIS_PASSWORD}

# Bellek yönetimi
maxmemory 256mb
maxmemory-policy allkeys-lru

# Persistence (cache-only senaryoda kapatılabilir)
save 900 1
save 300 10
save 60  10000
appendonly yes
appendfsync everysec

# Bağlantı
tcp-keepalive 300
timeout 0
